# 项目长期运维记忆

## 生产发布与回滚

执行任何正式服务器发布、数据库迁移、Docker 数据卷变更、生产备份或回滚前，必须先完整阅读：

`docs/superpowers/specs/2026-08-28-production-backup-rollback-design.md`

强制规则：

- 每次生产发布前必须创建并验证完整恢复点。
- 正常滚动保留最近 2 个已验证版本；新版本验收通过后才能清理最旧版本。
- 禁止在正式服务器执行 `docker compose down -v`。
- 禁止手工删除 MySQL、backend uploads 或 MinIO 数据卷。
- 禁止绕过备份验证直接执行 Prisma 生产迁移。
- 数据库迁移后的回滚必须同时匹配旧 Git 提交、旧数据库、旧文件卷和必要的旧环境配置，不能只回滚代码。
- 回滚前必须确认目标备份 ID、哈希、状态、Git 提交和迁移版本。
- 如果正式回滚脚本尚未实现或恢复演练尚未通过，停止生产发布并先完成脚本与演练，不得临场拼接破坏性命令。

当前方案只保存 ECS 本地副本，可用于版本迭代快速回滚，但不能防止 ECS 整机或云盘损坏。
